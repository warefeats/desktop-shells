{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Frontend may call the command surface and listen for push events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
