# The headline verdict is the geometric mean of idiomatic-path IPC ratios on the M2 Max

The page's one-sentence verdict comes from the IPC section, as the geometric mean over the idiomatic-path round-trip cells of the ratio of one candidate's median to the other's, on the Apple M2 Max run, with the Windows figure stated in the summary. The rule is fixed here, before any data exists, so the headline cannot be chosen after the fact from whichever section flatters one shell; every other section carries its own verdict by the statistic it displays. IPC leads because moving data across the boundary is the cost teams argue about and the one prior comparisons measure as a single echo, and the geometric mean is used because the payload sweep spans four orders of magnitude and an arithmetic mean would be the 16 MB cell alone.
